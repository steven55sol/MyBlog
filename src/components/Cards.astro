---
 const {src,tags,title,Url,authorUrl,NameAuthor,ImageAlt} = Astro.props;
---

<div class=" max-sm:w-full max-sm:flex-row max-sm:min-h-32 w-56 flex flex-col bg-white rounded-md shadow">
    <img 
       src={src} 
       alt={ImageAlt} 
       class="max-sm:w-32 max-sm:h-auto max-sm:rounded-l-md w-full h-32 object-cover sm:rounded-t-md "
    />
    <div class="p-3">
        <p class="text-sky-600 font-bold text-xs">{tags}</p>
       <a href={Url}><h3 class="mt-3 font-bold leading-5">{title}</h3></a>
        <div class="flex gap-1 items-center mt-4">
            <img 
              src={authorUrl} 
              alt=` Logo de ${NameAuthor}`
              class="w-5 h-5 rounded-full"
            />
            <p class="text-xs">{NameAuthor}</p>
        </div>
    </div>
</div>